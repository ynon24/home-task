{
  "name": "Bitcoin Rate - Enhanced Minimal",
  "image": "mcr.microsoft.com/devcontainers/base:bullseye",
  "features": {
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
  },
  "postCreateCommand": [
    "bash", "-c", 
    "curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash && curl -LO https://dl.k8s.io/release/stable.txt && curl -LO https://dl.k8s.io/release/$(cat stable.txt)/bin/linux/amd64/kubectl && sudo install kubectl /usr/local/bin/ && rm kubectl stable.txt && curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash && echo '🚀 Ready! Docker, Azure CLI, kubectl, and Helm installed'"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "ms-azuretools.vscode-azurecli"
      ]
    }
  },
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "remoteUser": "vscode"
}